# TODO: bring the 2026-09-25 USS work onto the comm board

The ultrasonic module (USS), the uss-link library and the bench streamer changed on
2026-09-25. The comm board firmware (`idf/`) has not been touched yet, apart from the
`uss_link.h` copy. This file lists what to do, in order. Background, measurements and bench
procedure: `Calibration-data-collection/calibration/USS.md`.

## What changed on the other side (read first)

| Where | Change | Effect on the comm board |
|---|---|---|
| module firmware | **ONE gas image for every cell** (`6a0290ff`). Capture window and ToF gate limits are **per cell and set by the ESP32 over I2C**; image defaults (window 110 µs = `gap_adcsmp` 1550, gate 80–320 µs) work on the 44 mm and the 75 mm cell until configured. On the 75 mm cell the window must be set to 140 µs (`gap_adcsmp` 1700): the zero then no longer depends on composition (±4 ns from 10 to 90 % CH4, was 5–54 ns). | **push the cell's window and gate at every module boot** (task 5b). Offsets measured before this are obsolete. |
| module firmware | gain fallback never goes below 25 | nothing to do |
| module firmware | **abs-ToF gate**: a capture with an implausible time of flight is rejected with **code 0xE1 (`USS_CODE_TOF_HOP`)**, counted as skipped in the raw totalizer, and triggers a re-search | it is a non-122 code, so the current driver already treats it as invalid. Up to 4 are expected after every module boot. |
| module firmware | **parameter access over I2C**: registers `0x44` (id), `0x45` (i32 value), commands `USS_CMD_PARAM_SET` 0x06 / `USS_CMD_PARAM_GET` 0x07, result block `0x68..0x6F` with its own CRC. **New since the first version of this file: `USS_PARAM_TOFG_MIN_NS` 0x41 / `USS_PARAM_TOFG_MAX_NS` 0x42** (ns, FRAM, SET range 20000–2000000, min < max). | additive, PROTO stays 3. Add the two gate ids to the preset sync (task 5b). |
| `uss_link.h` | all of the above | **already copied** into `idf/src/uss_link.h`; all three copies are byte-identical (MD5 `1f30b1de…`, LF line endings). Keep them in sync. |
| uss-link | `uss_param_get/set()`, and **`uss_derive.c/h`**: composition, gas properties, Re, K(Re, x), zero, low-flow cutoff, standard volumes, gas profiles | this is what the comm board should report (tasks 2–4) |

**Module settings persist in FRAM** across power cycles until the next flash. A module that
was tuned on the bench keeps those values. Read them back (`USS_CMD_PARAM_GET`) before
trusting a unit in the field.

## Tasks

### 1. Build check (do first; ~10 min)
- [x] Build `datalogger_idf` from `idf/` with the new `uss_link.h`. It is purely additive, so
      nothing should break. Watch for name clashes with the new `USS_PARAM_*` / `USS_PRM_*` macros.
      *Done 2026-09-25: builds clean (RAM 16.1 %, flash 62.4 %); the new macro names appear
      nowhere else in `idf/src`.*
- [x] Confirm `main.c`'s USS block handles `code == 0xE1` exactly like any non-122 code:
      no flow, the sample is kept, and it is logged.
      *Done 2026-09-25: nothing on the device branches on the code value. `uss_ok` is only
      "CRC-valid block read", so a 0xE1 sample is stored and `uss_code` is logged and uplinked.
      The module leaves it out of S1/S0. Note: its ToF/dTOF fields still go into the record and
      the JSON unfiltered, so anything downstream (server, and `uss_derive` in task 3) must gate
      on `uss_code == 122`.*

### 2. Decide: port to uss-link, or copy only the derivation
**Decided 2026-09-25: neither. The comm board stays separate from uss-link.** It keeps its own
driver (`idf/src/uss.c`, now with `uss_param_get/set`) and its own derivation
(`idf/src/gasflow.c/h`), written for the comm board. The constants and physics must stay
numerically identical to `uss_derive.c`; `tools/gasflow_check.c` checks that on the host
(2.8 M comparisons, worst relative difference 3.5e-16). **Refit one, refit both, bump
`GF_DERIVE_VER`, rerun the check.** uss-link's ESP-IDF port was not used: it caches its own
I2C handle, which `eco_i2c_hold_low()` (the rail-cycle recovery) would delete under it.

The original options, for reference:
The comm board has its **own** `idf/src/uss.c/.h`, a separate driver from `uss-link/src/uss.c`
with a different struct (`uss_sample_t`-style vs `uss_result_t`).
- **Option A (recommended): use uss-link as the ESP-IDF component** (its `CMakeLists.txt`
  already lists `uss.c uss_derive.c uss_port_espidf.c`) and delete the local driver. One
  driver, one place to fix bugs. Cost: re-verify retry/timeout behaviour on the real bus
  (clock stretching; `USS_XFER_TIMEOUT_MS` 1000), and the STALE-seq detection in `main.c`
  must keep working.
- **Option B: copy `uss_derive.c/h` only** and write a small adapter that fills a
  `uss_result_t` from the local struct (fields used: `code, tof_ups_q40, tof_dns_q40,
  dtof_ps, amp_ups, amp_dns`). Quicker, but it leaves two drivers drifting.

### 3. Derived values per record, WITHOUT changing the record layout
The record already holds every raw input: `uss_s1`, `uss_s0`, `uss_tot_skip`,
`uss_tof_ups_q40`, `uss_tof_dns_q40`, `uss_dtof_ps`, `uss_amp_*`, `press_dmbar`, `temp_cC`.
The MS5837 sits **in the gas line** (`config.h`), which is what the derivation needs.
- [x] In `uplink.c`, when building the JSON: `gf_sample()` from the record's sample
      fields plus `press_dmbar / 10` and `temp_cC / 100`, then `gf_window()` on
      `dS1 = s1 − prev.s1`, `dS0 = s0 − prev.s0`, `dt` = time between the two records.
- [x] The predecessor is **read back from the flash ring by seq** (`flashlog_read_seq`), not kept
      in RAM, so it survives deep sleep and reboots. No predecessor → no window
      (`uss_win_err: "no_prev"`), never zero.
- [x] Window rejected (with `uss_win_err`) on SAT, `dS0 <= 0` (includes a reset and pre-PROTO 3
      records), no usable dt, no valid sample at either end, **coverage** > 1.25, or mean
      |q| > 60 SL/min. Coverage is `dS0 · tu · td / 2^40 / dt`: the time S0 claims was
      integrated, over the wall clock. It stands in for "fits the number of captures", because the
      record has no TOT_N. Field median 0.990 (eco-field-02, 2026-09-23); not judged under 60 s.
- [x] `REC_MAGIC` unchanged (ECL6). A backlog logged before this build still gets derived values.
- [x] Raw keys unchanged and still sent.
- [x] Found and fixed along the way: when a batch overflowed the JSON buffer, `drain()` still
      advanced by the full batch, so records that did not fit were marked sent without ever
      leaving the board. Buffer is now 16 KB and the advance counts only what was sent.

Telemetry keys as implemented (per record, when the module answered):

| Key | Meaning |
|---|---|
| `uss_gas` | profile name (`biogas`, `air`, `air_co2`, `n2`) |
| `uss_c_mps` | speed of sound |
| `uss_x_a` | fraction of the profile's first component (**CH4 for biogas**) |
| `uss_q_std_lpm` | window-mean flow, standard L/min (20 °C / 1013.25 mbar), after the cutoff |
| `uss_v_std_l`, `uss_v_a_std_l` | window volume, total and first component (CH4) |
| `uss_re` | Reynolds number of the window |
| `uss_tof_bad` | the host judged the ToF impossible for the selected gas |
| `uss_cut` | the window was below the low-flow cutoff |
| `uss_derive_ver` | which constant set produced these numbers (bump on every refit) |
| `uss_validated` | the profile's constants were validated (biogas, air) |
| `uss_not_gas` | echo > 2× what this gas gives at this c (e.g. air in a biogas line); CH4 volume forced to 0. Replaces the proposed `uss_not_biogas`. |
| `uss_cov` | window coverage, see above (~1 when the module measured the whole window) |
| `uss_win_s` | window length, s |
| `uss_win_err` | why there is no window: `no_prev`, `no_module`, `saturated`, `ds0`, `no_dt`, `no_sample`, `coverage`, `window`, `q_max` |

Status record: `uss_preset` (-1 not tried this module boot, 0 failed, 1 in place).

### 4. Retire the legacy path
- [x] Removed the `K_est` diagnostic and its RTC state from `main.c`.
- [x] `uss_vol_ml` is still sent (the server reads it) and is commented as legacy in `uplink.c`.
      `uss_flow_ulpm` was already not uplinked.

### 5. Gas profile on the comm board
- [ ] NVS key `uss_gas` (default `biogas`), settable remotely (downlink / config message).
      **Blocked: the board has no downlink at all yet** (MQTT publish only). For now the profile
      is compile-time: `USS_GAS_PROFILE` in `config.h`.
- [x] Module preset synced once per module boot (`USS_ST_BOOT`, latched in `uss.c` because
      `uss_start_auto`'s command clears it) and after a cold boot of the comm board. GET first,
      SET only what differs, each SET verified by read-back. With biogas on a freshly flashed
      module this writes nothing. Tried once per module boot even on failure, so an old module
      without PARAM costs one 3 s timeout, not one per wake. *Not yet run on hardware.*
- [x] `uss_validated` reported in telemetry.
- [x] **Detect "not biogas"** (`uss_not_gas`): air reads as ~61 % CH4 by speed of sound
      alone. Use the echo amplitude (raw counts, `AMP_UPS/DNS`), normalised to gain 25: more
      than 2× the biogas amplitude for that speed of sound means not biogas (air ~1050 counts
      vs biogas 210–460 at gain 25). Publish a `uss_not_biogas` flag. Table and rule:
      `Calibration-data-collection/calibration/USS.md`, "Is it biogas at all?".

### 5b. Gas cell (NEW, 2026-09-25 evening)
The module no longer knows its cell; the ESP32 tells it. uss-link reference implementation:
`uss_cell_t`, `uss_derive_profile_cell(&cfg, gas, cell)`, and `uss_module_preset_t` gained
`tofg_min_ns` / `tofg_max_ns`, written by `uss_apply_module_preset()`.
*Done 2026-09-25 in idf-0.30 (first three items). eco-field-02 (COM125) is on the 44 mm cell and
is built with `USS_CELL GF_CELL_44`. The preset sync also re-runs when the cell, gas or
`GF_DERIVE_VER` changes (RTC key): idf-0.29 had possibly pushed the 75 mm window (1700) into
that module, and a reflash alone does not guarantee a cold boot. Server handoff:
`ecotrace-server/docs/CHANGES_FIRMWARE_idf-0.30.md`.*
- [x] **Mirror the cell in `gasflow.c`** (it carries these per cell, not per gas):

      | | 75 mm (validated) | 44 mm (NOT calibrated, starting point) |
      |---|---|---|
      | `path_m` | 0.07220 | 0.0440 |
      | `t0_us` | 1.2 | 1.2 |
      | K (`k_a`, `k_b`, `k_c`) | as fitted | × 44/72.2 (assumes the same 15.5 mm bore) |
      | `gap_adcsmp` | 1700 | 1550 |
      | ToF gate (ns) | 140000–320000 | 80000–200000 |
      | not-biogas amplitude check | on | **off** (`amp_ref_n = 0`): shorter path, stronger echo |
      | `validated` | per gas | 0 |

      Then rerun `tools/gasflow_check.c` against the new `uss_derive.c` and bump `GF_DERIVE_VER`.
      *Both cells × 4 gases: 5.6 M comparisons, 0 mismatches. `GF_DERIVE_VER` = 2.*
- [x] **Add the gate to the preset sync** (ids 0x41/0x42). Order matters: the module refuses a
      min ≥ current max, so widen first: SET max to 2000000, then min, then max
      (uss-link does exactly this).
- [x] Cell selection: compile-time `USS_CELL` in `config.h` next to `USS_GAS_PROFILE` (no
      downlink yet), later an NVS key `uss_cell`. Report it: telemetry key `uss_cell` (`44`/`75`).
- [ ] **Calibrate the 44 mm cell** before trusting its derived values: one air point (the
      acoustic temperature gives the real path L; the old 131.3 µs air reading implies ~1.7 %
      longer than 44 mm), a zero, a few flow points for K, and its own amplitude table. Expect a
      composition-dependent zero on this cell: its echo sits inside the transmit tail.

### 6. Per-unit constants, OTA-updatable
- [ ] Move the calibration constants into NVS with the compiled defaults as fallback:
      `k_a, k_b, k_c`, `off_a_ps`, `q_cutoff_lpm`, `path_m`, `t0_us`, `pipe_d_m`.
      Per unit, settable remotely.
- [ ] The low-flow cutoff (default 0.5 SL/min) applies to **derived** values only. Make sure
      nothing on the device cuts the raw sums.

### 7. Things the field will do that the bench did not
- [ ] **Water vapour.** Field biogas is saturated. At 35 °C ~5.6 % H2O shifts the CH4 reading
      by ~3–4 %. At least add a "saturated at T" option, or a humidity input if a sensor exists.
- [ ] **Temperature gradients** (sun on the pipe) moved the zero by 10–25 ns on the bench
      (2026-09-24 "hotgrad"), which is ~0.15–0.35 L/min. Log the gas temperature with every
      record; flag windows where the MS5837 temperature changes fast.
- [x] ~~The module's hard ToF limits~~: now I2C parameters, set per cell (task 5b).

### ✓ Answered: eco-field-02 (device 4) ToF
Its abs ToF of 132.5–133.0 µs (2026-09-23/24) is the **44 mm cell**, not the 75 mm one: air
reads ~131 µs there, and 132.5 µs is roughly 55–60 % CH4 with the nominal 44 mm path. The
`uss_tof_bad` flags came from applying 75 mm constants. Fix = task 5b (cell 44). The module on
the comm-board unit was flashed on 2026-09-25 with the standard image `6a0290ff`, whose
defaults (window 110 µs, gate 80–320 µs) work on this cell without any configuration.

### 8. Verify
- [x] Host equivalence check, from `tools/`:
      `gcc -std=c99 -O2 -Wall -Wextra -I../../uss-link/include -I../idf/src gasflow_check.c
      ../idf/src/gasflow.c ../../uss-link/src/uss_derive.c -lm -o gasflow_check && ./gasflow_check`
      → 0 mismatches (2026-09-25).
- [x] `datalogger_idf` builds clean (idf-0.29, RAM 18.0 %, flash 63.2 %).
- [ ] Bench: comm board + module on the MFC rig, one composition, 2/5/8 SL/min, ≥ 5 min per
      point. Compare `uss_v_std_l` per record against MFC-integrated standard litres. The
      expected agreement is a few %: 1.5 % rms in-sample on 2026-09-25, not yet validated
      out of sample.
- [ ] Soak at zero flow for ≥ 1 h: `uss_v_std_l` must stay 0 (cutoff), and `uss_skip`
      must not grow in steady gas (no 0xE1 after boot).

## Traps (from `uss-link/CLAUDE.md`, still valid)
- Build from `idf/`, not the repo root (different `platformio.ini`).
- ESP32 **automatic light sleep stays off** (it caused module reboots and code 135).
- **Flash order: host first, then the module**, whenever the contract changes. This change
  is additive, so either order works this time.
- `uss_link.h` exists in three copies: change all three together.
- Nothing is committed on 2026-09-25 by the assistant; the user does all commits.
- **Module image:** there is ONE gas image (`Firmware/tools/build.sh`, `BOARD=PCB I2CSLAVE=1`, LPM3 by
  default). Never flash a cell-specific build; there is no such thing any more. Flash only from WSL
  (MSP430Flasher), never with Windows TI tools.
