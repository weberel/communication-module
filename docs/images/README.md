# Board images

- `board_top.png` - top side (3D render from KiCad)
- `board_bottom.png` - bottom side (shows the BOOT / UART0 / test pads)

Higher-level renders and the schematic PDF are in [`../../hardware/exports/`](../../hardware/exports/).

TODO: add `board_annotated.png` - the top view with connector/pin callouts (the
hand-annotated render used during bring-up) if you want it embedded in the pinout doc.
